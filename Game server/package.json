{
  "name": "game-server",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "start": "node index.js",
    "generate:client": "./node_modules/.bin/ag \"../AsyncAPI/GameServer.yaml\" @asyncapi/ts-nats-template --install --force-write --output \"./GeneratedNatsClient\" && cd ./GeneratedNatsClient && npm i && npm run build && cd .."
  },
  "dependencies": {
    "NatsClient": "file:GeneratedNatsClient",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@asyncapi/generator": "1.6.13"
  },
  "author": "Jonas Lagoni (jonas-lt@live.dk)",
  "license": "Apache 2.0"
}
